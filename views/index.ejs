<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LMS Platform - Home</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f0f9ff',
                            100: '#e0f2fe',
                            200: '#bae6fd',
                            300: '#7dd3fc',
                            400: '#38bdf8',
                            500: '#0ea5e9',
                            600: '#0284c7',
                            700: '#0369a1',
                            800: '#075985',
                            900: '#0c4a6e',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        .card-hover {
            transition: all 0.3s ease;
        }

        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        .feature-icon {
            transition: all 0.3s ease;
        }

        .feature-icon:hover {
            transform: scale(1.1);
        }
        .fade-message {
            animation: fadeOut 3s forwards;
        }

        @keyframes fadeOut {
            0% { opacity: 1; }
            70% { opacity: 1; }
            100% { opacity: 0; }
        }
    </style>
</head>

<body class="font-sans">
    <!-- Navigation -->
    <nav class=" bg-slate-400 sticky top-0 z-50">
        <div class=" mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <div class="flex-shrink-0 flex items-center">
                       <a href="/"> <img src="/images/logo.png" alt="LMS" class="w-10 h-10 object-cover rounded-full"></a>
                        <span class="ml-2 text-2xl font-bold text-zinc-800 lg:ml-4">Learning Management System</span>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/student/signin">
                        <button class="text-gray-700 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors">
                            Sign In
                        </button>
                    </a>
                    <a href="/signup"
                        class="bg-gradient-to-r from-blue-500 to-cyan-500 text-white px-4 py-2 rounded-lg hover:from-blue-600 hover:to-cyan-600 transition-all duration-300 inline-block">
                        Sign Up
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="bg-gradient-to-tr from-orange-100 to-green-100">
        <div>
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
                <div class="text-center">
                    <h1 class="text-5xl font-bold text-zinc-800 mb-6">
                        Transform Your Learning Experience
                    </h1>
                    <p class="text-xl text-gray-800 mb-8 max-w-3xl mx-auto">
                        Access premium courses from expert instructors, complete certifications, and advance your skills
                        with our comprehensive learning management system.
                    </p>
                    <div class="flex flex-col sm:flex-row justify-center gap-4">

                         <button onclick="showAdminLoginModal()"
                            class="bg-indigo-600 text-white px-8 py-3 border-2 border-white rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition-all duration-300">
                            Admin Login
                        </button>

                        <a href="/signup">
                            <button
                                class="bg-rose-400 border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition-all duration-300">
                                Sign Up Now
                            </button>
                        </a>
                        
                        <a href="/instructor/instructor_signin"
                            class="bg-blue-600 text-white px-8 py-3 rounded-lg font-semibold border-2 border-white hover:bg-white hover:text-blue-600 transition-all duration-300 inline-block">
                            Login as Instructor
                        </a>
                       
                    </div>
                    
                </div>
            </div>

          
        
            <!-- Admin Login Modal -->
            <div id="adminLoginModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
                <div class="bg-white p-8 rounded-xl shadow-xl w-96">
                    <h2 class="text-2xl font-bold text-gray-800 mb-4 text-center">Admin Login</h2>
                    <form action="/admin/login" method="POST" class="space-y-4">
                        <div>
                            <label class="block text-gray-700 font-medium mb-2">Password</label>
                            <input type="password" name="password" required
                                class="w-full border border-gray-300 p-3 rounded-lg focus:ring-2 focus:ring-blue-300 focus:border-blue-400 outline-none">
                        </div>
                        <div class="flex gap-3">
                            <button type="button" onclick="closeAdminLoginModal()"
                                class="flex-1 bg-gray-500 text-white py-3 px-4 rounded-lg hover:bg-gray-600 transition">
                                Cancel
                            </button>
                            <button type="submit"
                                class="flex-1 bg-blue-600 text-white py-3 px-4 rounded-lg hover:bg-blue-700 transition">
                                Login
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        
            </div>
    </div>

    <!-- Courses Section -->

    <div class="py-16 bg-gradient-to-b from-orange-200 via-green-100 to-yellow-100">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-gray-900 mb-4">Available Courses</h2>
                <p class="text-gray-700 max-w-2xl mx-auto">Choose from our curated selection of 5 premium courses taught
                    by expert instructors.</p>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 justify-items-center">
                <% courses.forEach((course)=>{ %>
                    <div class="bg-white rounded-xl shadow-sm overflow-hidden card-hover w-full max-w-xs">
                        <div class="h-48 bg-gray-200 flex items-center justify-center">
                           <% 
                                let imageSrc = "/images/default.jpg";

                                if (course.image && course.image.trim() !== "") {
                                    imageSrc = course.image;
                                } 
                                else if (course.image_data && course.image_data.data) {
                                    const base64 = course.image_data.data.toString('base64');
                                    imageSrc = `data:${course.image_data.contentType};base64,${base64}`;
                                }
                             %>

                            <img src="<%= imageSrc %>" class="w-full h-full object-cover">
                        </div>
                        <div class="p-6">
                            <div class="flex justify-between items-start mb-2">
                                <h3 class="text-xl font-semibold text-gray-900">
                                    <%=course.title %>
                                </h3>
                                <span class="bg-blue-100 text-blue-800 text-sm px-2 py-1 rounded">
                                    <%=course.level %>
                                </span>
                            </div>
                            <p class="text-gray-600 text-sm mb-4">
                                <%=course.description %>
                            </p>
                            <div class="flex justify-between items-center">
                                <span class="text-lg font-bold text-blue-600">$<%=course.price %></span>
                                <a href="/student/student_dashboard">
                                    <button
                                    class="bg-gradient-to-r from-blue-500 to-cyan-500 text-white px-4 py-2 rounded-lg hover:from-blue-600 hover:to-cyan-600 transition-all duration-300 text-sm">
                                    Enroll Now
                                   </button>
                                </a>
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>


    <!-- Footer -->
    <footer class="bg-gray-900 text-white">
        <div class="border-t border-gray-800 pt-4 pb-4 text-center text-gray-400  ">
            <p>&copy; 2025 EduPlatform. All rights reserved.</p>
        </div>

    </footer>



    <script>

        function showAdminLoginModal(){
            document.getElementById('adminLoginModal').classList.remove('hidden');
        }
        
        function closeAdminLoginModal(){
            document.getElementById('adminLoginModal').classList.add('hidden');
        }
        




    </script>
</body>


</html>