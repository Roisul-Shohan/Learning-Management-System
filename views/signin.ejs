<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sign In | LMS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .fade-message {
            animation: fadeOut 3s forwards;
        }

        @keyframes fadeOut {
            0% { opacity: 1; }
            70% { opacity: 1; }
            100% { opacity: 0; }
        }
  </style>
</head>
<body class="font-sans">
    <!-- Navigation -->
    <nav class="bg-slate-400 sticky top-0 z-50">
        <div class=" mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <div class="flex-shrink-0 flex items-center">
                         <a href="/"> <img src="/images/logo.png" alt="LMS" class="w-10 h-10 object-cover rounded-full"></a>
                        <span class="ml-2 text-2xl font-bold text-zinc-800">Learning Management System</span>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/signup" class="text-zinc-700 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors font-bold text-lg">
                        Sign Up
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-green-100 to-indigo-200 px-4">

  <div class="bg-gradient-to-tl from-slate-500 via-cyan-100 to-gray-400  rounded-2xl shadow-3xl p-8 w-full max-w-md border border-green-100">

    <div class="flex justify-center mb-6">
      <img src="/images/logo.png" alt="LMS" class="w-20 h-20 object-cover rounded-full">
    </div>

    <div class="text-center mb-6">
      <h1 class="text-2xl sm:text-3xl font-extrabold text-gray-900 mb-2">Welcome Back!</h1>
      <p class="text-gray-600 text-sm">Continue your learning journey with our comprehensive Learning Management System</p>
    </div>

    <h2 class="text-xl font-bold text-gray-900 mb-6 text-center">Sign In</h2>
    <form action="/student/signin" method="POST" class="flex flex-col gap-4">
      <div class="flex flex-col">
        <label for="email" class="mb-1 font-medium text-gray-700">Email</label>
        <input type="email" name="email" id="email" placeholder="example@gmail.com"
          class="px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-400" required>
      </div>
      <div class="flex flex-col">
        <label for="password" class="mb-1 font-medium text-gray-700">Password</label>
        <input type="password" name="password" id="password" placeholder="********"
          class="px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-400" required>
          <% if(error) { %>
              <p class="text-sm text-red-700 text-center fade-message">❌<%=error%></p>
            <%}%>
        </div>
    
      <button type="submit"
        class="mt-4 px-6 py-3 bg-orange-500 text-white font-semibold rounded-full shadow-md hover:bg-orange-600 transition duration-300">
        Sign In
      </button>
    </form>
    

    <p class="mt-6 text-center text-gray-600 text-sm">
      Don't have an account?
      <a href="/signup" class="text-orange-500 font-semibold hover:underline">Sign Up</a>
    </p>

  </div>

</body>
</html>